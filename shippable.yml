#language setting
language: node_js

# version numbers
node_js:
  - "4.4.3"

env:
  global:
    - OPENSHIFT_REPO=ssh://5744c3980c1e669a680000bd@altomandov3-lbarreraabarca.rhcloud.com/~/git/altomandov3.git/
    
before_install:
  - git remote -v | grep ^openshift || git remote add -m master openshift $OPENSHIFT_REPO

after_success:
  - git push -f openshift $BRANCH:master

install:
  - npm install
